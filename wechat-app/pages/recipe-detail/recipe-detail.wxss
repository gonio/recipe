/* 详情页面样式 */

/* WebView 全屏容器 */
.web-view-container {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 9999;
  background: #fff;
}

.web-view-container web-view {
  width: 100%;
  height: 100%;
}

/* WebView 返回按钮 */
.web-view-back {
  position: fixed;
  top: 20rpx;
  left: 20rpx;
  z-index: 10000;
  display: flex;
  align-items: center;
  padding: 16rpx 24rpx;
  background: rgba(0, 0, 0, 0.6);
  border-radius: 40rpx;
}

.web-view-back image {
  width: 32rpx;
  height: 32rpx;
  margin-right: 8rpx;
}

.web-view-back text {
  color: #fff;
  font-size: 28rpx;
}

/* 顶部大图 */
.hero-section {
  position: relative;
  width: 100%;
  height: 500rpx;
  margin: -30rpx -30rpx 0;
  width: calc(100% + 60rpx);
}

.hero-image {
  width: 100%;
  height: 100%;
}

.hero-image.placeholder {
  background: linear-gradient(135deg, #E3F2FD, #BBDEFB);
}

/* 视频类型菜谱样式 */
.video-hero-section {
  position: relative;
  width: 100%;
  height: 420rpx;
  margin: -30rpx -30rpx 0;
  width: calc(100% + 60rpx);
  background: #000;
}

.video-player {
  width: 100%;
  height: 100%;
}

/* 平台视频封面 */
.video-cover {
  width: 100%;
  height: 100%;
}

/* 播放遮罩 */
.play-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.4);
}

.play-btn {
  width: 120rpx;
  height: 120rpx;
  background: rgba(255, 255, 255, 0.9);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 20rpx;
}

.play-btn image {
  width: 48rpx;
  height: 48rpx;
  margin-left: 8rpx;
}

.play-text {
  color: #fff;
  font-size: 28rpx;
  font-weight: 500;
}

/* 打开平台按钮 */
.open-platform-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--primary-color);
  color: #fff;
  padding: 28rpx;
  border-radius: 44rpx;
  margin-top: 30rpx;
}

.open-platform-btn.secondary {
  background: var(--bg-light);
  border: 2rpx solid var(--border-color);
}

.open-platform-btn.secondary text {
  color: var(--text-primary);
}

.open-platform-btn image {
  width: 32rpx;
  height: 32rpx;
  margin-right: 12rpx;
}

.open-platform-btn text {
  font-size: 30rpx;
  font-weight: 500;
}

.video-info-section {
  background: var(--bg-white);
  border-radius: 30rpx 30rpx 0 0;
  margin: -40rpx -30rpx 30rpx;
  padding: 40rpx 30rpx;
  position: relative;
  z-index: 1;
}

.note-section {
  background: var(--bg-light);
  border-radius: 16rpx;
  padding: 24rpx;
  margin: 24rpx 0;
}

.note-label {
  display: block;
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 12rpx;
}

.note-content {
  font-size: 28rpx;
  color: var(--text-secondary);
  line-height: 1.8;
}

/* 视频样式 */
.hero-video {
  width: 100%;
  height: 100%;
}

.video-badge {
  display: flex;
  align-items: center;
  background: rgba(0, 0, 0, 0.6);
  color: #fff;
  padding: 8rpx 20rpx;
  border-radius: 24rpx;
  margin-left: 16rpx;
  font-size: 24rpx;
}

.video-badge image {
  width: 24rpx;
  height: 24rpx;
  margin-right: 8rpx;
}

.hero-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 200rpx;
  background: linear-gradient(to top, rgba(0,0,0,0.5), transparent);
  display: flex;
  align-items: flex-end;
  padding: 30rpx;
}

.cuisine-tag {
  background: rgba(255, 255, 255, 0.95);
  color: var(--text-primary);
  padding: 12rpx 28rpx;
  border-radius: 28rpx;
  font-size: 26rpx;
  font-weight: 500;
}

.back-btn {
  position: absolute;
  top: 60rpx;
  left: 30rpx;
  width: 72rpx;
  height: 72rpx;
  background: rgba(255, 255, 255, 0.9);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);
}

.back-btn image {
  width: 36rpx;
  height: 36rpx;
}

/* 信息区域 */
.info-section {
  background: var(--bg-white);
  border-radius: 30rpx 30rpx 0 0;
  margin: -40rpx -30rpx 30rpx;
  padding: 40rpx 30rpx;
  position: relative;
  z-index: 1;
}

.recipe-name {
  display: block;
  font-size: 48rpx;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 24rpx;
}

.recipe-meta {
  display: flex;
  gap: 40rpx;
  margin-bottom: 24rpx;
}

.meta-item {
  display: flex;
  align-items: center;
  font-size: 26rpx;
  color: var(--text-secondary);
}

.meta-item image {
  width: 32rpx;
  height: 32rpx;
  margin-right: 10rpx;
  opacity: 0.6;
}

/* 标签 */
.tags-section {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin-bottom: 30rpx;
}

.tag {
  background: var(--primary-light);
  color: var(--primary-dark);
  padding: 10rpx 24rpx;
  border-radius: 24rpx;
  font-size: 24rpx;
}

/* 收藏操作 */
.favorite-action {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 28rpx;
  background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
  border-radius: 44rpx;
  box-shadow: 0 8rpx 24rpx rgba(66, 165, 245, 0.3);
}

.favorite-action.active {
  background: linear-gradient(135deg, #EF5350, #E53935);
  box-shadow: 0 8rpx 24rpx rgba(229, 57, 53, 0.3);
}

.favorite-action image {
  width: 36rpx;
  height: 36rpx;
  margin-right: 12rpx;
}

.favorite-action text {
  color: white;
  font-size: 30rpx;
  font-weight: 600;
}

/* 通用区块 */
.section {
  background: var(--bg-white);
  border-radius: 20rpx;
  padding: 30rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 20rpx var(--shadow-color);
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.section-title {
  font-size: 34rpx;
  font-weight: 700;
  color: var(--text-primary);
  position: relative;
  padding-left: 20rpx;
}

.section-title::before {
  content: '';
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 6rpx;
  height: 32rpx;
  background: var(--primary-color);
  border-radius: 3rpx;
}

.section-count {
  font-size: 24rpx;
  color: var(--text-light);
}

/* 食材列表 */
.ingredients-list {
  display: flex;
  flex-direction: column;
}

.ingredient-item {
  display: flex;
  align-items: center;
  padding: 20rpx 0;
  border-bottom: 1rpx solid var(--border-color);
}

.ingredient-item:last-child {
  border-bottom: none;
}

.ingredient-dot {
  width: 12rpx;
  height: 12rpx;
  background: var(--primary-color);
  border-radius: 50%;
  margin-right: 20rpx;
}

.ingredient-name {
  flex: 1;
  font-size: 30rpx;
  color: var(--text-primary);
}

.ingredient-amount {
  font-size: 28rpx;
  color: var(--text-secondary);
}

/* 步骤列表 */
.steps-list {
  display: flex;
  flex-direction: column;
  gap: 30rpx;
}

.step-item {
  display: flex;
  gap: 20rpx;
}

.step-number {
  width: 56rpx;
  height: 56rpx;
  background: var(--primary-color);
  color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 600;
  flex-shrink: 0;
}

.step-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.step-image {
  width: 100%;
  height: 300rpx;
  border-radius: 16rpx;
  object-fit: cover;
}

.step-text {
  font-size: 30rpx;
  color: var(--text-primary);
  line-height: 1.8;
}

/* 来源信息 */
.source-section {
  text-align: center;
  padding: 30rpx;
}

.source-label {
  font-size: 24rpx;
  color: var(--text-light);
}

.bottom-spacer {
  height: 140rpx;
}

/* 底部固定栏 */
.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: var(--bg-white);
  display: flex;
  padding: 20rpx 30rpx 40rpx;
  box-shadow: 0 -4rpx 20rpx rgba(0, 0, 0, 0.05);
  z-index: 100;
}

.bottom-action {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24rpx;
  border-radius: 40rpx;
  font-size: 28rpx;
}

.bottom-action.favorite {
  flex: 1;
  background: var(--primary-light);
  padding: 24rpx 40rpx;
}

.bottom-action.favorite image {
  width: 32rpx;
  height: 32rpx;
  margin-right: 12rpx;
}

.bottom-action.favorite text {
  color: var(--primary-dark);
  font-weight: 500;
}

.bottom-action.favorited {
  flex: 1;
  background: #FFEBEE;
  padding: 24rpx 40rpx;
}

.bottom-action.favorited image {
  width: 32rpx;
  height: 32rpx;
  margin-right: 12rpx;
}

.bottom-action.favorited text {
  color: #E53935;
  font-weight: 500;
}
