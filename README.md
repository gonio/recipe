# 🍳 美味食谱小程序

一个智能菜谱微信小程序，由 Kimi Claw 自动搜罗各地美食菜谱，让你每天都能发现新美味。

## ✨ 功能特性

### 用户端功能
- 📚 **我的菜谱** - 收藏喜爱的菜谱，按菜系分类管理
- 🏪 **市场** - 浏览 Kimi Claw 搜罗的新菜谱，一键收藏
- ⭐ **今日推荐** - 智能推荐今日食谱，解决"今天吃什么"的烦恼
- 🔍 **智能搜索** - 支持按菜名、食材搜索
- 🎯 **偏好设置** - 定制喜欢的菜系，获取精准推荐

### 管理端功能（Kimi Claw）
- 🤖 **自动爬取** - 定时从互联网搜罗菜谱
- 📊 **智能分类** - 自动按菜系分类整理
- 🔔 **每日更新** - 每天推送新菜谱到用户首页
- 🌟 **推荐算法** - 根据用户偏好智能推荐

## 🏗️ 项目架构

```
recipe-miniapp/
├── backend/              # 后端 API 服务
│   ├── models/          # MongoDB 数据模型
│   ├── routes/          # API 路由
│   ├── controllers/     # 业务逻辑控制器
│   ├── middleware/      # 中间件
│   ├── config/          # 配置文件
│   └── server.js        # 入口文件
├── wechat-app/          # 微信小程序前端
│   ├── pages/           # 页面文件
│   ├── images/          # 图片资源
│   ├── app.js           # 小程序入口
│   └── app.wxss         # 全局样式
└── kimi-claw-scripts/   # Kimi Claw 爬虫脚本
    ├── crawler.js       # 菜谱爬取脚本
    └── daily-crawl.js   # 定时任务脚本
```

## 🚀 快速开始

### 1. 部署后端服务

```bash
cd backend

# 安装依赖
npm install

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填写数据库和微信小程序配置

# 启动服务
npm start
```

后端服务默认运行在 `http://localhost:3000`

### 2. 部署微信小程序

1. 打开微信开发者工具
2. 导入 `wechat-app` 目录
3. 修改 `app.js` 中的 `apiBaseUrl` 为你的后端地址
4. 配置小程序的 AppID
5. 编译预览

### 3. 配置 Kimi Claw 爬虫

```bash
cd kimi-claw-scripts

# 安装依赖
npm install

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填写 API_BASE_URL 和 API_KEY

# 测试运行
npm start

# 启动定时任务（每天 9 点自动执行）
npm run daily
```

## 📱 页面说明

| 页面 | 说明 |
|-----|------|
| 首页 | 展示用户收藏的菜谱，按菜系筛选 |
| 市场 | 展示 Kimi Claw 搜罗的新菜谱 |
| 今日推荐 | 系统智能推荐的今日食谱 |
| 我的 | 个人中心、偏好设置、统计信息 |
| 菜谱详情 | 展示菜谱详细信息、食材、步骤 |

## 🎨 UI 设计

- **主色调**: 淡蓝色系 (#42A5F5)
- **风格**: 简洁清新
- **设计原则**: 
  - 卡片式布局
  - 圆角设计
  - 柔和阴影
  - 清晰的信息层级

## 🔧 环境变量配置

### 后端 (.env)

```
MONGODB_URI=mongodb://localhost:27017/recipe_miniapp
JWT_SECRET=your-jwt-secret
WECHAT_APPID=your-wechat-appid
WECHAT_SECRET=your-wechat-secret
API_KEY=your-secure-api-key
PORT=3000
```

### Kimi Claw (.env)

```
API_BASE_URL=http://your-server:3000/api
API_KEY=your-secure-api-key
```

## 📡 API 接口

### 认证相关
- `POST /api/auth/wechat-login` - 微信登录
- `GET /api/auth/user` - 获取用户信息
- `PUT /api/auth/preferences` - 更新偏好菜系

### 菜谱相关
- `GET /api/recipes/favorites` - 获取收藏列表
- `GET /api/recipes/market` - 获取市场菜谱
- `GET /api/recipes/daily-recommend` - 获取今日推荐
- `GET /api/recipes/detail/:id` - 获取菜谱详情
- `POST /api/recipes/favorite` - 收藏菜谱
- `POST /api/recipes/unfavorite` - 取消收藏
- `GET /api/recipes/search` - 搜索菜谱
- `POST /api/recipes/admin/create` - 创建菜谱（管理员）

## 🗄️ 数据模型

### User（用户）
```javascript
{
  openid: String,           // 微信 OpenID
  nickname: String,         // 昵称
  avatarUrl: String,        // 头像
  preferredCuisines: [],    // 偏好菜系
  favorites: [],            // 收藏的菜谱ID
  viewedRecipes: []         // 已查看的菜谱ID
}
```

### Recipe（菜谱）
```javascript
{
  name: String,             // 菜名
  cuisine: String,          // 菜系
  imageUrl: String,         // 图片
  ingredients: [],          // 食材
  steps: [],                // 步骤
  cookTime: Number,         // 烹饪时间
  difficulty: Number,       // 难度
  tags: [],                 // 标签
  favoriteCount: Number,    // 收藏数
  isDailyRecommended: Boolean // 是否今日推荐
}
```

## 🤖 Kimi Claw 使用说明

在 Kimi Claw 中设置定时任务：

```
每天早上 9 点，执行以下任务：
1. 运行菜谱爬虫脚本，搜罗新的菜谱
2. 按菜系分类保存到数据库
3. 设置今日推荐菜谱
4. 如果有新增菜谱，推送给用户
```

Kimi Claw 会：
- 自动从互联网搜索菜谱
- 解析菜谱信息（名称、食材、步骤等）
- 保存到你的后端服务器
- 用户打开小程序就能看到新菜谱

## 📝 更新日志

### v1.0.0
- ✅ 基础功能：收藏、市场、推荐、搜索
- ✅ Kimi Claw 自动爬取
- ✅ 用户偏好设置
- ✅ 淡蓝清新 UI 风格

## 🙏 致谢

- 感谢 Kimi 提供的 AI 能力
- 感谢 Kimi Claw 的自动化支持

## 📄 许可证

MIT License
